import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mdcd.polyclinique',
  appName: 'MDCD Polyclinique',
  webDir: 'out',
};

export default config;
